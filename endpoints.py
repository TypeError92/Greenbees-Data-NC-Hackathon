endpoints = {

    # /API

    "GET /api": {
        "description": "Serves up a json representation of all available endpoints.",
        "access": ["user", "app", "admin"],
        "queries": {}
    }
}
